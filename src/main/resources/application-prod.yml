spring:
  data:
    redis:
      cluster:
        nodes: ${redis.cluster.nodes:?redis.cluster.nodes is required in prod profile}
        max-redirects: 3
      password: ${redis.password:}
      ssl:
        enabled: ${redis.ssl.enabled:false}
      timeout: 5000
